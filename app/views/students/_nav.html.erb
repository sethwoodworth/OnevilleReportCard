<% pages = ["general", "attendance", "grades", "tests", "comments"] %>
<div id="footer">
  <ul id="bottom-navigation" class="">
      <% pages.each do |page| %>
        <% if page == params[:action] %>
          <li class="clickable active" id="<%="#{page}"%>-link">
            <%= link_to page.capitalize, "#{page}" %>
          </li>
        <% else %>
          <li class="clickable" id="<%="#{page}"%>-link">
            <%= link_to page.capitalize, "#{page}" %>
          </li>
        <% end %>
      <% end %>
  </ul>
</div>
